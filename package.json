{
  "name": "secure-json-logic",
  "version": "0.9.11",
  "description": "Use logic-objects from uncertain sources and run them locally without breaking the own system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pubkey/secure-json-logic.git"
  },
  "keywords": [
    "json",
    "logic",
    "secure"
  ],
  "license": "BSD-2-Clause",
  "dependencies": {
    "url": "^0.11.0"
  }
}
